{
  "name": "@cityq/coreq",
  "version": "0.1.0",
  "private": true,
  "description": "coreQ — BFF / orchestration API (Node.js Fastify)",
  "type": "module",
  "scripts": {
    "dev": "node --watch src/index.js",
    "build": "node scripts/sync-dist.mjs",
    "start": "node dist/index.js",
    "lint": "eslint src --ext .js"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "fastify": "^5.1.0",
    "@fastify/cors": "^10.0.1",
    "@fastify/helmet": "^12.0.1"
  },
  "devDependencies": {
    "@types/node": "^22.10.0"
  }
}
