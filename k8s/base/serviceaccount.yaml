apiVersion: v1
kind: ServiceAccount
metadata:
  name: erpq-sa
  namespace: erpq
# For private registries (e.g. GHCR), set an imagePullSecret in the overlay and reference it here.
# imagePullSecrets:
#   - name: ghcr-pull

