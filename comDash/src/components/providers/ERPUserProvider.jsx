'use client';

/**
 * Re-export portal session provider (CityQ JWT + optional ERP profile).
 * @see ../../ui/providers/ERPUserProvider.jsx
 */
export { ERPUserProvider, useERPUser } from '../../ui/providers/ERPUserProvider';
