{
  "name": "@cityq/docq",
  "version": "0.1.0",
  "private": true,
  "description": "docQ — Zoho WorkDrive document workflows (Fastify + Postgres)",
  "type": "module",
  "scripts": {
    "dev": "node scripts/copy-frappe.mjs && node --watch src/index.js",
    "build": "node scripts/copy-frappe.mjs && node scripts/sync-dist.mjs",
    "start": "node dist/index.js",
    "lint": "eslint src --ext .js"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "@fastify/cors": "^10.0.1",
    "@fastify/jwt": "^10.0.0",
    "@fastify/multipart": "^9.0.1",
    "amqplib": "^0.10.4",
    "fastify": "^5.1.0",
    "pg": "^8.16.3"
  },
  "devDependencies": {
    "@types/node": "^22.10.0"
  }
}
