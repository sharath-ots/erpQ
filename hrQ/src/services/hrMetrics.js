/**
 * Placeholder for HR metrics fetched from apiGate.
 * Implement actual ERPNext API calls here as needed.
 */
export async function fetchHrMetrics({ apiBase, getAccessToken } = {}) {
  return {
    totalEmployees: null,
    openPositions: null,
    pendingLeaves: null,
  };
}
