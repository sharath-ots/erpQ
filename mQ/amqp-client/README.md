# `@cityq/mq-client`

Optional shared helpers on top of `amqplib`. Import from **coreQ** or workers when you want one place for exchange/queue assertions and publishing conventions.

Build: `npm install && npm run build`.
