{
  "name": "@cityq/mq-client",
  "version": "0.1.0",
  "private": true,
  "description": "Thin AMQP helpers for publishers/consumers (optional shared lib)",
  "type": "module",
  "main": "./dist/index.js",
  "scripts": {
    "build": "node scripts/sync-dist.mjs",
    "lint": "eslint src --ext .js"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "amqplib": "^0.10.4"
  },
  "devDependencies": {
    "@types/amqplib": "^0.10.5",
    "@types/node": "^22.10.0"
  }
}
